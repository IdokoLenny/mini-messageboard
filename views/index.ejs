<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    
    <% messages.forEach((message) => { %>
        <h1><%= message.user %></h1>
        <h1><%= message.text %></h1>
        <h1><%= message.added %></h1>
        <hr>
    <% }) %>

    <p><a href="/new">Click</a> to add new user</p>
</body>
</html>